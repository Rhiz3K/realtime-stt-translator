# Deps for downloading + inspecting the Parakeet-tdt-0.6b-v3 browser-engine model
# assets via scripts/prepare_parakeet_onnx.py. Unlike the Nemotron prep, no ONNX
# conversion is needed (a ready int8 export is downloaded as-is), so this is just
# the HF downloader plus onnx for header-only I/O inspection.
# Use Python 3.12 — onnx wheels may lag on newer interpreters.
huggingface_hub
onnx
